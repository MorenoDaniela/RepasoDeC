#ifndef FECHA_C_INCLUDED
#define FECHA_C_INCLUDED
/*
int fechaValida (Fecha f)
{
    int bufferDia;
    int bufferMes;
    int bufferAnio;


    if(utn_getInSimple("Fecha de nacimiento: Ingrese dia:" ))
        int dias_mes[] = {0, 31, 28, 31, 30,
                         31, 30, 31, 31,
                         30, 31, 30, 31};

    if (f.mes < 1 || f.mes > 12)
        return 0;
    if (f.dia < 1 || f.dia > dias_mes[f.mes])
        return 0;
}
*/

#endif // FECHA_C_INCLUDED
